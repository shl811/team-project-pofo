const sliderWrap = document.querySelector(".slider-wrap");
const slider = sliderWrap.querySelector(".slider");
const slides = sliderWrap.querySelectorAll(".slider li");
let currentIndex = 0;
let slideCount = slides.length;
let slideWidth = 200;
let slideMargin = 30;

const sliderControls = document.querySelector(".slider-controls");
const prevButton = sliderControls.querySelector(".prev-button");
const nextButton = sliderControls.querySelector(".next-button");

function makeColne(){
    for(let i = 0; i<slideCount; i++){
        
    }
}