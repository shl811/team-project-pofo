<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="com.pofo.pofoapiboot3.repository.RecruitmentPostRepository">

    <insert id="insert" parameterType="RecruitmentPost">
        insert into recruitment_post(member_id, title, location_type, location_info, period, team_size, thumbnail)
        values(#{memberId}, #{title}, #{locationType}, #{locationInfo}, #{period}, #{teamSize}, #{thumbnail})
    </insert>
</mapper>
    